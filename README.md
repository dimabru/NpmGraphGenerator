Graph Generator for npm dependncies

To use run:

`npm install` 

`npm start -- -p | --packageName [package name] -v [version (optional, defaults to latest)]`

The program prints dependency tree to the console
